<UserControl x:Class="WPFLightMotor.Controls.GameControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFLightMotor.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.InputBindings>
        <KeyBinding Gesture="P" Command="{Binding PauseCommand}"></KeyBinding>
        <KeyBinding Gesture="CTRL+S" Command="{Binding SaveCommand}"></KeyBinding>
        <KeyBinding Gesture="ESC" Command="{Binding EscapeCommand}"></KeyBinding>
    </UserControl.InputBindings>
    <Grid>
        
    </Grid>
</UserControl>
